(function() {

  var options = {
    strings: ["Get your message across", "Thank us <i>aftewords</i>"],
    typeSpeed: 70,
    backSpeed: 40,
    backDelay: 1000,
    loop: true
  }

  var typed = new Typed("#typed", options);

})();
